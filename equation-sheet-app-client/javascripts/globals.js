const baseUrl = 'http://localhost:3000';
const fieldUrl = `${baseUrl}/api/fields`
const equationsUrl = `${baseUrl}/api/equations`
// var Fraction = algebra.Fraction;
// var Expression = algebra.Expression;
// var _Equation = algebra.Equation;